# Repository Instructions

Use neutral and scientific reasoning.

This repository implements a local deterministic participation gate. Do not add LLM calls, cloud dependencies, API-key requirements, live-service tests, or hidden evaluators to the core package.

Core tests must remain deterministic and network-free.
